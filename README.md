# E-Commerce Web Application

## 📌 Description
This project is a Node.js based E-Commerce web application built using Express and MongoDB.  
It allows users to browse products, register and log in, and manage basic e-commerce operations.

The application follows the MVC (Model–View–Controller) architecture for better organization and scalability.

## 🛠 Technologies Used
- Node.js
- Express.js
- MongoDB (Mongoose)
- EJS / HTML (Views)
- JavaScript
- Express Session
- bcrypt (Password hashing)
  

## ✨ Features
- User registration and authentication
- Product listing
- MVC project structure
- Secure password handling
- Server-side rendering
- Database integration using MongoDB
  
  
## ⚙️ Installation & Running the Project
1. Install **Node.js** and **MongoDB**
2. Clone or download the repository
3. Open the project folder in a terminal
4. Run the following commands:

```bash
npm install
npm start

